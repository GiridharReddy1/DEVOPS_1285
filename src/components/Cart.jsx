export default ({cart,rem,inc,dec,clear,total})=><div className="cart"><h2>Shopping Cart</h2>{cart.length===0?<p>Cart is Empty</p>:cart.map(i=><div key={i.id}><b>{i.name}</b> - ₹{i.price} x {i.qty}<br/><button className="qty" onClick={()=>dec(i.id)}>-</button><button className="qty" onClick={()=>inc(i.id)}>+</button><button className="remove" onClick={()=>rem(i.id)}>Remove</button><hr/></div>)}<h3>Total: ₹{total}</h3>{cart.length>0&&<button className="remove" onClick={clear}>Clear Cart</button>}</div>;